repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    def vertxVersion = rootProject.property("vertx_version")

    api "io.vertx:vertx-web:$vertxVersion"

    api project(":vertlin-boot")
}
